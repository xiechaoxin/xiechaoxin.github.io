---
title: 6.函数
category: 
  - CPP
  - C++ primer
  - 1-CPP基础
tags:
  - C++ primer
  - 1-CPP基础
date: 2022-07-09 03:04:36
---



# 6.函数

## 6.1 函数基础

函数是一个命名了的代码块，通过调用函数执行相应的代码。可以有0个或多个参数，可以重载。

函数的调用：

* 用实参初始化函数对应的形参（类型、个数需要匹配）
* 主函数暂时中断，被调函数开始执行

#### 局部对象

* 自动对象：生命周期从变量声明开始，到函数块末尾结束
* 局部静态对象：生命周期从变量声明开始，直到程序结束才销毁

#### 函数声明

* 函数需要先声明，再定义
* 一般在头文件中声明函数

#### 分离式编译

编译和运行分开，可以在不同机器上进行



## 6.2 参数传递

### 传值参数

### 传引用参数

#### 使用引用避免深拷贝



#### 使用引用形参返回额外信息



### const形参和实参

* 形参的顶层const会被忽略掉
  * 这个函数承诺不会修改参数，那么我传入const或者非const的参数都无所谓，所以顶层const会被忽略
* 指针或引用形参与const，底层const不允许忽略
  * 这个函数需要参数是一个指针，通过指针修改对应地址的值，你给我指向常量(const)的指针，我不接受

> 如果函数无需改变引用形参的值，最好将其声明为常量引用。

### 数组形参

C++允许将变量定义成数组的引用

> 以数组作为形参的函数必须确保使用数组时不会越界

### main：处理命令行选项

```cpp
int main(int argc, char *argv[])
```



### 含有可变形参的函数

含有可变形参的函数：参数个数不固定
如果所有的实参类型相同，可以传递一个名为initializer_list的标准库类型

省略符形参：只能出现在形参的最后，仅用于C++访问某些特殊的C代码

## 6.3 返回类型和return 语句



### 无返回值函数



### 有返回值函数

> 不要返回局部对象的引用或指针

#### 返回类类型的西数和调用运算符

#### 引用返回左值

如果返回类型是常量引用，则不可以给调用的结果赋值

#### 列表初始化返回值

#### 主函数main的返回值

在必要的时候编译器会为main加上return 0；

#### 递归：函数调用了自己

main函数不能调用自己



### 返回数组指针

数组不能拷贝，所以函数不能直接返回数组



## 6.4 函数重载

函数名称相同但形参列表不同

### 重载与作用域

* 定义重载函数
* const_ cast和重载

一且在当前作用域中找到了所需的名字，编译器就会忽略掉外层作用域中的同名实体，内层优先级高于外层

## 6.5 特殊用途语言特性

### 默认实参

一旦某个形参被赋了默认值，它后面的所有形参都必须有默认值

### 内朕函数和constexpr函数

#### 内联函数

* 在每个调用点上“内联地”展开，避免函数调用的开销
* 内联说明只是向编译器发出的一个请求，编译器可以选择忽略这个请求
* 现在一般不写，因为这个是编译器决定的，写inline只是建议，不写编译器也可能做这样的优化

#### constexpr 函数

* 能用于常量表达式的函数：函数的返回类型以及所有的形参都是字面值类型
* 函数体中必须有且只有一条return语句
* constexpr函数被隐式地指定为内联函数
* constexpr函数并不要求返回常量表达式

### 调试帮助

- 调试帮助：只在开发过程中使用的代码，发布时屏蔽掉
- assert预处理宏：cassert头文件中，用于断言测试
- 用于检测“不能发生”的条件
- 开发阶段使用，发布阶段需要去除



## 6.6 函数匹配

寻找最佳匹配、不能具有二义性



编译器将实参类型到形参类型的转换划分成几个等级：

1. 精确匹配
2. 通过const转换实现的匹配
3. 通过类型提升实现的匹配
4. 通过算术类型转换实现的匹配（所有算术类型转换的级别都一样）
5. 通过类类型转换实现的匹配

## 6.7 函数指针

指针指向的是函数

```cpp
bool (*pf)(const string &,const string &)
```

> 在指向不同函数类型的指针间不存在转换规则

#### 重载函数的指针

函数本身作为一个类型（函数指针），必须精确匹配



#### 函数指针形参

不能定义函数类型的形参，但是形参可以是指向函数的指针

```cpp
void useBigger(const string &s1,bool (*pf)(const string &,const string &))
```

#### 返回指向函数的指针

不能返回函数，但可以返回指向函数的指针（和函数类型的形参不一样，返回类型必须写成指针形式）

```cpp
int (*f1(int))(int *, int);
```

常用auto和decltype用于函数指针类型

